<?xml version="1.0" encoding="utf-8"?>
<!--
   Backup rules to exclude sensitive data from Android backups for devices older than API 31.
   See https://developer.android.com/guide/topics/data/autobackup for details.
-->
<full-backup-content>
    <!-- Exclude Keystore-encrypted preferences and encrypted database to prevent AEADBadTagException on restore -->
    <exclude domain="sharedpref" path="db_security_prefs.xml"/>
    <exclude domain="sharedpref" path="secure_auth_prefs.xml"/>
    <exclude domain="database" path="hometutorpro.db"/>
    
    <!-- Include user settings from DataStore -->
    <include domain="file" path="datastore/"/>
</full-backup-content>
